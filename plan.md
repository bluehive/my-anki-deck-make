
### ?? GrokビルドCLIへ与える指示書（プロンプト）


```text
あなたは学習効率化の専門家であり、優秀なプログラマーです。
以下の【要件】と【タスクリスト】に従い、暗記アプリ「Anki」にインポートするための「Git学習デッキ」と「高校数学デッキ」のデータ、およびそれを自動生成するスクリプトを作成してください。

【共通の設計ルール】
1. ワーキングメモリの限界を考慮し、1問あたりの情報量は「5秒以内に瞬時に答えられるサイズ」に抑える。ただし、各デッキ内の文や式は極端に短く断片化しすぎず、**やや大き目**（自然で読みやすい文・完全な式単位）で記述すること。
2. データ形式はAnkiにインポート可能な「カンマ区切り（CSV）」形式を基本とすること。

【要件1：Gitデッキ（50問）】
・テーマ：Gitの基本操作、よくある間違い、ローカル/リモートリポジトリの設定。
・形式：3列CSV（「表面」「裏面」「Image」）。
  - 各カードに**対応するミニマルイラスト**を1枚添付する。
  - イラストは `simple-icon-set.jpg` をスタイル参照とし、限定24色パレットのフラットで極めてシンプルなアイコン調とする。
  - ファイル名例：`git-001-init.png`
  - 画像は `assets/images/git-deck/` ディレクトリに保存。
  - インポート時に画像は `storeMediaFile` でAnkiに取り込まれ、Frontに `<img>` として表示される。
・出力ファイル名：`git_deck.csv`

【要件2：数学デッキ（100問）】
・テーマ：高校3年生レベル（基礎解析の目次に基づく：指数関数と対数関数、三角関数、複素数、微分、無理関数など）。
・形式：公式の暗記に特化した「穴埋め（Cloze）カード」形式で作成すること。
・Anki固有の記法ルール：
  - 穴埋め部分は `{{c1::隠したい文字や数式}}` のフォーマットを使用すること。
  - 数式はMathJaxを利用し、インライン数式は `[$]...[/$]` 、独立行の数式は `[$$]...[/$$]` で囲むこと。
  （例： `[$]\lim_{x \to 0} \frac{\sin x}{x} = {{c1::1}}[/$]` ）
・出力ファイル名：`math_deck.csv`

【要件3：AnkiConnect自動化スクリプト】
・作成したCSVファイルを読み込み、ローカルで起動しているAnki（AnkiConnect経由）に「Gitデッキ」と「数学デッキ」を自動作成してカードを追加するPythonスクリプトを作成すること。

それでは、以下の【タスクリスト】の順に処理を実行し、完了状況を報告してください。
```

---

### ?? タスクリスト

Grokが段階的に作業を進められるように、上記のプロンプトと一緒に以下のタスクリストを渡してください。

```text
【タスクリスト】
- [ ] Task 1: `git_deck.csv` の生成
  - Gitに関する50問のQ&Aを作成し、CSVファイルとしてローカルに保存する。
- [ ] Task 2: `math_deck.csv` の生成
  - 三角関数、指数・対数関数、無理関数、複素数、微分などの範囲で100問を作成する。
  - MathJax記法（[$]...[/$]）とCloze記法（{{c1::...}}）を正しく適用し、CSVファイルとしてローカルに保存する。
- [ ] Task 3: `import_to_anki.py` (Pythonスクリプト) の作成
  - `requests` ライブラリを使用してAnkiConnectのAPI（ポート8765）を叩き、デッキの作成とCSVからのカード追加を行うスクリプトを記述・保存する。
- [ ] Task 4: 成果物の確認と使い方ガイドの出力
  - 生成されたファイルの一覧と、ユーザーが実行すべき次のステップ（Ankiアプリ側の準備やスクリプトの実行コマンド）をターミナルに出力する。
```


*   **AnkiConnectの活用**: 150問のデータを手動で取り込むのは手間がかかるため、PythonとAnkiConnectを使った自動インポートスクリプト の作成もタスクに含めています。これにより、GrokにPythonコードを書かせ、CLI上で実行するだけでAnkiへの登録が完了します。



Ankiアプリで学習するための「Gitの基本操作・よくある間違い・設定」に関する50問のデッキリストを作成しました。

問題の草案は以下。かならず、参照してデッキ作りに活用すること。

### Git操作・設定 Ankiデッキ用問題リスト（全50問）

#### ソース資料に基づく問題（23問）
| 表面（問題） | 裏面（答え） |
| --- | --- |
| カレントディレクトリに新しくGitリポジトリを作成するコマンドは？ | `git init` |
| URLやパスを指定して、既存のリポジトリをカレントディレクトリにコピーするコマンドは？ | `git clone <url_or_path>` |
| 現在のワーキングディレクトリの変更状態（ステータス）を表示するコマンドは？ | `git status` |
| カレントディレクトリのすべての変更を、コミットの準備領域（ステージングエリア）に追加するコマンドは？ | `git add .` |
| 追加した変更にメッセージを付けて、ローカルリポジトリに記録するコマンドは？ | `git commit -m "メッセージ"` |
| リモートリポジトリの最新の変更を取得し、ローカルに統合（プル）するコマンドは？ | `git pull` |
| ローカルリポジトリで記録した変更を、リモートリポジトリに送信（プッシュ）するコマンドは？ | `git push` |
| Gitを利用してコミットを行うために、最初に設定しなければならない2つの情報は？ | ユーザー名とメールアドレス |
| Gitのコミットに記録されるデフォルトのユーザー名を設定するコマンドは？ | `git config --global user.name "名前"` |
| Gitのコミットに記録されるデフォルトのメールアドレスを設定するコマンドは？ | `git config --global user.email "メールアドレス"` |
| `git config --global` コマンドで設定したユーザー名などの情報が保存されるファイルはどこか？ | `~/.gitconfig` |
| 現在のローカルリポジトリの状態に、バージョンなどの目印（タグ）をつけるコマンドは？ | `git tag "バージョンやラベル"` |
| 1つのリポジトリで、複数の作業ディレクトリを競合なく切り出すことができるGitの機能は？ | `git worktree` |
| 指定したパスに新しいワークツリーを作成し、同時に新しいブランチを作成してチェックアウトするコマンドは？ | `git worktree add <パス> -b <ブランチ名>` |
| 誤コミットを防ぐため、ワークツリーを作成する際に安全な場所はどこか？ | リポジトリ外（隣接ディレクトリなど） |
| 現在作成されているワークツリーの一覧を確認するコマンドは？ | `git worktree list` |
| ワークツリーを作成した際、`.gitignore`で除外されているファイル（例:`node_modules`や`.env`）はどうなるか？ | 新しいワークツリーには含まれない（シンボリックリンクや個別インストールが必要） |
| 不要になったワークツリーを削除するコマンドは？ | `git worktree remove <パス>` |
| `git worktree remove` 実行時、未コミットの変更が残っているワークツリーはどうなるか？ | 安全装置が働き、デフォルトでは削除されない |
| 未コミットの変更があるワークツリーを強制的に削除するためのオプションは？ | `--force` |
| ワークツリーのディレクトリを直接削除してしまい、`list` に管理情報が残った場合、それを掃除するコマンドは？ | `git worktree prune` |
| ブランチを削除しようとして拒否される原因のうち、ワークツリーに関連するものは？ | どこかのワークツリーでそのブランチがHEADとして開かれているため |
| 同じブランチを2つのワークツリーで同時に開くことはできるか？ | できない（1ブランチ = 1ワークツリーがGitの仕様） |

#### 一般的なGit知識で補った問題（外部知識：27問）
| 表面（問題） | 裏面（答え） |
| --- | --- |
| ワーキングディレクトリとステージングエリアの差分を表示するコマンドは？ | `git diff` |
| ステージングエリアと最新コミットの差分を表示するコマンドは？ | `git diff --staged` (または `--cached`) |
| これまでのコミット履歴を表示するコマンドは？ | `git log` |
| コミット履歴を1行ずつ簡潔に表示するオプションは？ | `git log --oneline` |
| 特定のコミットで行われた変更内容を確認するコマンドは？ | `git show <コミットID>` |
| ローカルリポジトリに存在するブランチの一覧を表示するコマンドは？ | `git branch` |
| 現在のコミットから新しいブランチを作成するコマンドは？（切り替えはしない） | `git branch <ブランチ名>` |
| 指定したブランチに作業を切り替えるコマンドは？ | `git checkout <ブランチ名>` (または `git switch`) |
| 新しいブランチを作成し、同時にそのブランチに切り替えるコマンドは？ | `git checkout -b <ブランチ名>` (または `git switch -c`) |
| 指定したブランチの変更を、現在のブランチに統合するコマンドは？ | `git merge <ブランチ名>` |
| マージ済みの不要なブランチを削除するコマンドは？ | `git branch -d <ブランチ名>` |
| まだマージされていないブランチを強制的に削除するコマンドは？ | `git branch -D <ブランチ名>` |
| まだコミットしていないワーキングディレクトリの変更を取り消すコマンドは？ | `git restore <ファイル名>` (または `git checkout -- <ファイル名>`) |
| すでに `git add` したファイルをステージングエリアから取り除く（アンステージする）コマンドは？ | `git restore --staged <ファイル名>` (または `git reset HEAD <ファイル名>`) |
| 直前のコミットの内容を修正したり、コミットメッセージを書き直したりするコマンドは？ | `git commit --amend` |
| 直前のコミットを取り消すが、作業した変更内容はステージングエリアに残すコマンドは？ | `git reset --soft HEAD~` |
| 直前のコミットと、そこに含まれる作業内容を完全に破棄して元に戻すコマンドは？ | `git reset --hard HEAD~` |
| 過去の特定のコミットで行われた変更を「打ち消す」ための新しいコミットを作成するコマンドは？ | `git revert <コミットID>` |
| 登録されているリモートリポジトリのURL一覧を表示するコマンドは？ | `git remote -v` |
| 新しいリモートリポジトリを `origin` という名前で登録するコマンドは？ | `git remote add origin <URL>` |
| リモートリポジトリの最新情報を取得するが、自分のローカルファイルにはまだ統合させないコマンドは？ | `git fetch` |
| ローカルのブランチを初めてリモートにプッシュし、今後 `git pull` だけで追跡できるように設定するコマンドは？ | `git push -u origin <ブランチ名>` |
| まだコミットしたくない中途半端な変更を、一時的に退避させるコマンドは？ | `git stash` |
| 退避させた作業（スタッシュ）の一覧を表示するコマンドは？ | `git stash list` |
| 最後に退避させた作業を復元し、退避リストから削除するコマンドは？ | `git stash pop` |
| 【よくある間違い】大容量ファイルやパスワードを含むファイルを誤ってコミットした場合、単純に `rm` で削除してコミットすれば履歴からも完全に消えるか？ | 消えない（履歴から完全に消すには `git filter-repo` などの専用ツールによる歴史の書き換えが必要） |
| 【よくある間違い】`git push` をした際 "rejected (non-fast-forward)" エラーが出た。主な原因と対処法は？ | 原因: リモート側に自分の知らない新しいコミットがある。<br>対処法: 先に `git pull` でリモートの変更を取り込んでからプッシュする |

---


### 数学基礎解析　100問デッキ構成案（目次＆ソース資料準拠）

**1. 指数計算・指数関数・対数関数（約20問）**
第1章および第3章の内容です。計算ミスを防ぐための基礎を徹底します。
*   **指数法則と拡張**: $a^{-n} = \frac{1}{a^n}$、$a^0 = 1$、$a^{\frac{1}{2}} = \sqrt{a}$ などの基本変換。
*   **累乗根（指数根）の性質**: $\sqrt[n]{b^m} = b^{\frac{m}{n}}$ といった分数指数への変換。
*   **奇数乗根と偶数乗根の違い**: $\sqrt{(-6)^2} = 6$ だが、$\sqrt{-27} = -3$ のように、$n$乗根の$n$が奇数か偶数かによって性質が異なる点の注意。
*   **常用対数とその応用**: 桁数や最高位の数を求める問題の解法手順（※手順を穴埋めにして暗記）。

**2. 三角関数（基礎?公式?グラフ）（約40問）**
第4章の内容です。覚えるべき公式が非常に多いため、Ankiの穴埋めカードを最大限に活用します。
*   **基本性質**: $\tan\theta$ の定義域（漸近線は $\theta \neq \frac{\pi}{2} + n\pi$）や、$\sin\theta, \tan\theta$ は奇関数（原点対称）、$\cos\theta$ は偶関数（y軸対称）であること。
*   **グラフの変形**: $y = \sin(k\theta)$ の周期は $\frac{2\pi}{k}$ になるなど、グラフの拡大・縮小・平行移動のルール。
*   **公式群の穴埋め暗記**: 加法定理、倍角・半角の公式、三角関数の合成、積和・和積の公式。
    *   *作成のコツ*: 右辺をまるごと、あるいは符号や係数だけを隠すカードを作ります。

**3. 複素数とn乗根（約10問）**
第5章の内容です。指数根（n乗根）を複素数平面に拡張した内容を含めます。
*   **ド・モワブルの公式**: $(\cos\theta + i\sin\theta)^n = \cos(n\theta) + i\sin(n\theta)$ の穴埋め。
*   **複素数のn乗根**: 複素数平面上での解の配置（正n角形になる幾何学的性質など）。
*   **オイラーの公式**: $e^{i\theta} = \cos\theta + i\sin\theta$ （指数関数と三角関数の結びつき）。

**4. 微分と極限（三角関数・指数関数）（約30問）**
第6章（および数III）の内容です。計算の「型」を暗記します。
*   **極限の公式**: $\lim_{x \to 0} \frac{\sin x}{x} = 1$、$\lim_{x \to 0} \frac{\tan x}{x} = 1$。
*   **極限の計算テクニック**: 分母や分子に $1-\cos x$ がある場合、$1+\cos x$ を掛けて $\sin^2 x$ を作り出す解法パターン。
*   **導関数の公式**: $(\sin x)' = \cos x$、$(\cos x)' = -\sin x$、$(\tan x)' = \frac{1}{\cos^2 x}$。
*   **合成関数の微分**: $(\sin 2x)' = 2\cos 2x$ や $(\tan^2 x)' = \frac{2\tan x}{\cos^2 x}$ などの、置き換えを伴う微分手順。

---

### デッキ作成時のAnki活用ルール

この100問をAnkiで運用する際、資料に記載されている**「資格試験や高度な学習に向けたAnkiのコツ」**を導入すると、記憶の定着が飛躍的に高まります。

1.  **問題は「5秒以内で答えられる」サイズに分解する**
    「加法定理をすべて答えよ」という重いカードは作らず、$\sin(\alpha+\beta)$、$\cos(\alpha-\beta)$ など、公式1つにつき1枚のカードに分割します。ただし、各文や式は**やや大き目**（自然で読みやすい完全な表現）に保ち、極端に短く断片化しすぎないようにする。テンポよく解けることを最優先としつつ、意味の通じやすいサイズを心がける。
2.  **ショートカットで「Cloze（穴埋め）カード」を量産する**
    数式の覚えたい部分を選択し、キーボードの `Ctrl + Shift + C` (Macは `Cmd + Shift + C`) を押すだけで、簡単に穴埋め問題が作れます。
3.  **関連する論点は1つのノートにまとめる（ワントピックノート）**
    例えば「倍角の公式（$\cos 2\theta$ の3パターン）」は、1つのノート内に複数の穴埋めとしてまとめて作成します。回答時に「他のパターンはどうだったかな？」と気になった際、同じ画面ですぐに周辺知識を確認できて便利です。
4.  **MathJaxで数式を美しく表示する**
    カード内で数式を書くときは `[$]` と `[/$]` で囲むことで、きれいに表示できます（例：`[$] \lim_{x \to 0} \frac{\sin x}{x} = 1 [/$]`）。特別なインストール不要で使えるため、高校数学のデッキ作成には必須の機能です。

目次から全体像（地図）を把握し、その中の細かい公式や計算の「型」をAnkiで確実に定着させるというアプローチは、理系数学の学習において最強のメソッドになります。ぜひこの構成案をベースに作成を進めてみてください。

---

## Grokによる理解・解析・実装プラン・仕様定義（2026-07-07 追加）

### 1. プロジェクト全体の使命（Mission）の理解

本プロジェクト「anki-mydick-make」の核心的な目的は以下の通り：

- **学習効率化の専門家として**、Ankiを活用した「高速・高定着」の暗記システムを構築する。
- ワーキングメモリの限界を厳密に考慮し、**1問あたり5秒以内で瞬時に答えられる**範囲に収めつつ、各カード内の文や数式は**やや大き目**（自然な文や完全な式で構成）とする。極端な断片化は避ける。
- Gitと高校数学（基礎解析）の2領域について、**手作業ゼロ**でAnkiに取り込める状態まで自動化する。
- 成果物として、以下の3点を必ず生成する：
  1. `git_deck.csv`（50問・Basic形式）
  2. `math_deck.csv`（100問・Cloze + MathJax形式）
  3. `import_to_anki.py`（AnkiConnect経由の完全自動インポートスクリプト）

**【重要・新規定】カード内の文・式サイズ**
- 各デッキ内の**文や式のサイズはやや大き目**にすること。
- 極端に短く切り刻んだ断片ではなく、自然で読みやすい長さの文や、完全な数式・コマンド表現を1単位として使用する。
- 5秒ルールは厳守するが、「短すぎて意味が取りにくい」カードは避ける。

この指示書（plan.md）は、**Grok自身に対するプロンプト**としても機能しており、将来的に同じ指示を繰り返し与えても一貫した成果物が得られるように設計されている。

### 2. 要件の詳細解析

#### 【要件1】Gitデッキ（50問）
- **形式**: 3列CSV（「表面」「裏面」「Image」）
- **ビジュアル追加**:
  - 各50問に対して**1枚ずつミニマルなイラスト**を作成する。
  - スタイル参照: `assets/images/simple-icon-set.jpg`（限定24色・フラット・極小アイコン調・クリーンなベクター風）。
  - 各イラストは対応するGit概念を**視覚的に一目でわかる**レベルで表現（人物・動物・PC・クラウドなどの要素を参考にしつつ、Git操作に特化してシンプルに）。
  - 画像ファイルは `assets/images/git-deck/git-XXX-xxx.png` の形式で保存（XXXは001から050の連番）。
- **対象範囲**:
  - 基本操作（init, clone, add, commit, push, pull, status など）
  - 設定（user.name / user.email, ~/.gitconfig）
  - ワークツリー（git worktree の全操作）
  - ブランチ操作
  - 取り消し・修正操作（reset, revert, restore, commit --amend）
  - リモート操作、stash
  - **よくある間違い**の事例（大容量ファイル誤コミット、non-fast-forward など）
- **既存素材**: plan.md内にソース資料23問＋一般知識27問の完全リストが既にテーブル形式で提供されている。これを**一次資料**として活用し、品質確認・微調整の上でCSV化する。
- **設計原則**: 5秒以内で答えられる範囲を守りつつ、質問文・答え文はやや大き目（自然な説明や完全なコマンド例を含む形）で記述する。極端に短い断片は避ける。イラストも「一瞬で概念を想起できる」ミニマルさとする。

#### 【要件2】数学デッキ（100問）
- **形式**: **Cloze（穴埋め）カード専用**（Ankiの `{{c1::}}` 記法必須）
- **数式表現**:
  - インライン: `[$] ... [/$]`
  - ディスプレイ（独立行）: `[$$] ... [/$$]`
- **主要トピックと配分目安**（基礎解析目次準拠）:
  - 指数計算・指数関数・対数関数：約20問
  - 三角関数（性質・グラフ・加法定理・倍角・合成など）：約40問
  - 複素数とn乗根（ド・モアブル、オイラー公式）：約10問
  - 微分と極限（三角・指数関数）：約30問
- **作成方針**:
  - 公式1つにつき基本的に1カード（または1ノートに論理的に関連する複数穴埋め）。
  - 「計算の型」「頻出の変換パターン」を優先的に穴埋め化。
  - 例: `[$]\lim_{x \to 0} \frac{\sin x}{x} = {{c1::1}}[/$]`
- **既存素材**: トピック別構成案のみ。**具体的な100問の作成が必要**。

#### 【要件3】AnkiConnect自動化スクリプト
- **言語**: Python
- **必須ライブラリ**: `requests`
- **AnkiConnect API**（localhost:8765）:
  - デッキ作成: `createDeck`
  - ノート追加: `addNote`（繰り返し実行）
  - ノートタイプ:
    - Git → `"Basic"` （将来的にImageフィールド追加も検討）
    - 数学 → `"Cloze"`
  - フィールド (Git):
    - `Front`: 質問文 + `<img src="画像ファイル名.png">` （画像を視覚補助として前面に表示）
    - `Back`: 答え文
  - 画像の扱い:
    - AnkiConnect の `storeMediaFile` を使って画像をbase64でアップロード
    - CSVの `Image` カラムからファイル名を取得し、`assets/images/git-deck/` 内のファイルを読み込む
    - 画像は各カードの学習を助けるミニマルビジュアルとして機能（コマンドの概念を一瞬で想起）
- **機能要件**:
  - CSV読み込み（`csv`標準モジュール使用、適切なquoting）
  - デッキが存在しない場合は自動作成
  - 進捗表示・エラーハンドリング
  - 重複追加防止の工夫（可能なら）
- **前提条件**: Anki本体が起動中 + AnkiConnectアドオンが有効（ポート8765）であること。
- **出力**: ユーザーが実行するだけで全カードがAnkiに取り込まれる状態。

#### 【共通設計ルール】厳守事項
1. 情報量は「5秒以内に答えられる」サイズに収める。ただし、**各デッキ内の文や式は「やや大き目」**とする。
   - 極端に短く断片化しすぎず、自然で読みやすい完全な文や式単位で記述する。
   - 例: 「`git add .`」だけではなく「カレントディレクトリのすべての変更をステージングエリアに追加するコマンドは？」のような自然な質問文を許容する。
2. Ankiインポート可能なCSVを基本形式とする（最終的にはスクリプトで直接addNoteするため、CSVは中間成果物としても重要）。
3. 日本語表記を一貫させ、コード部分はバッククォートや適切な記法で視認性を高める。

### 3. 実装プラン（タスクリスト対応）

| タスク | 内容 | 優先 | 完了基準 |
|--------|------|------|----------|
| Task 1 | `git_deck.csv` の生成 | 高 | 50行＋各問対応のミニマルイラスト50個生成（simple-icon-set.jpgスタイル、24色限定）。CSVは3列（表面,裏面,Image）。画像はassets/images/git-deck/に保存。 |
| Task 2 | `math_deck.csv` の生成 | 高 | 100行、Cloze+MathJaxが正しく記述、配分バランス良好 |
| Task 3 | `import_to_anki.py` の作成 | 高 | requests使用、AnkiConnectでデッキ+カード+画像メディア追加成功。Gitカードは画像をFrontに埋め込んで表示。 |
| Task 4 | 成果物確認 + 使い方ガイド出力 | 中 | ファイル一覧、次にユーザーが行う手順（Anki準備、pip install、python実行）が明確にターミナル表示 |

**推奨作業順序**:
1. 仕様確定（本セクションの追記）
2. Gitデッキのビジュアル仕様確定 + ミニマルイラスト生成（simple-icon-set.jpg参照）
3. GitデッキCSVを3列に更新（Imageカラム追加） ← 現在ここ（16個生成済みで一旦確定）
4. 数学デッキ
5. import_to_anki.py を画像対応（storeMediaFile + Frontに<img>埋め込み）に拡張
6. 残りイラスト生成を継続しながら統合テスト
7. ガイド整備

### 4. 技術仕様（Technical Specifications）

#### 4.1 ファイル構成（最終目標）
```
anki-mydick-make/
├── plan.md
├── git_deck.csv                    # Git 50問（3列：表面,裏面,Image）
├── math_deck.csv
├── assets/
│   └── images/
│       ├── simple-icon-set.jpg     # スタイル参照用
│       └── git-deck/               # 50個のミニマルイラスト
│           ├── git-001-init.png
│           ├── git-002-clone.png
│           └── ...
├── import_to_anki.py
├── requirements.txt
└── README.md or USAGE.md
```

#### 4.2 CSVフォーマット詳細
- **git_deck.csv**（3列構成）
  ```csv
  "表面","裏面","Image"
  "カレントディレクトリに新しくGitリポジトリを作成するコマンドは？","`git init`","git-001-init.png"
  ...
  ```
  - `Image` カラムにはファイル名のみを記載（パスはスクリプト側で `assets/images/git-deck/` と結合）。
  - Anki側では画像をメディアとして添付し、カードのFrontまたはBackに表示する形を想定（例: 表面にイラスト + 質問、裏面に答え）。
- **math_deck.csv**
  - カラム構成は柔軟（スクリプト側で扱いやすい形）。
  - 推奨: `"Text"` カラムのみ、または `"Front"` としてClozeテキストを格納。
  - 例行: `"[$]\lim_{x \to 0} \frac{\sin x}{x} = {{c1::1}}[/$]"`

#### 4.3 AnkiConnect 呼び出し仕様（スクリプトで使用する主なアクション）
- `createDeck`
- `addNote`
- 可能であれば `deckNames`, `modelNames` で事前確認
- Noteパラメータ例（Clozeの場合）:
  ```json
  {
    "deckName": "高校数学・基礎解析",
    "modelName": "Cloze",
    "fields": {
      "Text": "[$](\\sin x)' = {{c1::\\cos x}}[/$]"
    },
    "tags": ["math", "基礎解析", "微分"]
  }
  ```

#### 4.4 品質基準（カードレベル）
- 各カードは**1つの事実・1つの公式・1つのコマンド**に限定。
- ただし、**文や数式のサイズはやや大き目**とする（自然な長さの文、完全な公式表現を優先）。
- 数学カードは**計算手順の「型」**を意識。
- MathJaxはAnki標準でレンダリング可能であることを確認。
- 日本語の自然さと正確性の両立。

#### 4.5 環境・プラットフォーム考慮
- 実行環境: Windows（PowerShell）想定（ユーザ情報より）
- 改行コード: CRLF または LF（Python csvは柔軟）
- パス処理: 相対パス優先
- 文字コード: UTF-8（BOMなしを推奨。Ankiインポート時は注意）

### 5. リスク・注意点・制約

- **AnkiConnect未起動**: スクリプト実行時に明確なエラーメッセージを出す。
- **数式の正確性**: 特に三角関数の加法定理・微分公式はミス厳禁。作成後にダブルチェック。
- **Cloze記法ミス**: `{{c1::}}` の記述ミスはAnki上で壊れる。生成ロジックで堅牢化。
- **重複カード**: スクリプト側で簡易チェック、またはユーザーに「最初に空のデッキで実行」指示。
- **Ankiのノートタイプ**: 標準の "Basic" と "Cloze" が存在することを前提。カスタムモデルは避ける。
- **大規模変更時の巻き戻し**: スクリプトは「追加専用」とし、削除は手動に留める。

### 6. 完了の定義（Definition of Done）

- [ ] `git_deck.csv` が50問で存在し、3列構成（表面,裏面,Image）
- [ ] `assets/images/git-deck/` に50個のミニマルイラストが揃っている（simple-icon-set.jpgと同一スタイル・24色限定）
- [ ] 各イラストが対応するGit概念を視覚的に表現している
- [ ] `math_deck.csv` が100問で存在し、Cloze + MathJax記法が正しい
- [ ] `import_to_anki.py` が画像メディア（storeMediaFile）も含めてAnkiConnect経由で正しく追加でき、Gitカードで画像がFrontに表示される
- [ ] スクリプト実行後、Ankiアプリ上で画像付きカードが正しく表示・学習可能であること
- [ ] Task 4 のガイドがターミナルに明確に出力される
- [ ] plan.md に本仕様セクションが追記済み（本タスク完了）

### 7. 今後の進め方（本セッション以降）

1. 本仕様をplan.md末尾に確定させる（現在進行中）。
2. ユーザーの承認を得てから、Task 1〜4を順次実行。
3. 各タスク完了ごとに中間成果物を報告。
4. 最終的に、ユーザーが `python import_to_anki.py` を実行するだけでAnkiに150問が取り込まれる状態を目指す。

この解析に基づき、以降の作業は**すべてこの仕様に準拠**して進めること。

---

## 進捗記録（2026-07-08）

### 実行したこと（本セッション）
- plan.md への複数回の仕様更新
  - カード内の文・式のサイズを「やや大き目」に変更（極端な断片化を避ける）
  - Gitデッキにビジュアルを追加する要件を新設（simple-icon-set.jpg をスタイル参照、限定24色パレット・フラットミニマルアイコン）
  - CSVを3列構成（表面 / 裏面 / Image）に変更
  - 画像保存場所・命名規則・AnkiConnectでの扱い方（storeMediaFile + Frontへの `<img>` 埋め込み）を詳細化
  - Task 1〜3、DoD、ファイル構成、進め方をビジュアル対応に更新
- Task 1 の実行
  - `generate_git_deck.py` を作成・更新（3列CSV出力 + 画像マッピング管理）
  - `git_deck.csv` を生成・更新（16個の画像を正しく対応付け）
  - simple-icon-set.jpg のスタイルを参考に、Gitの各概念に対応した**ミニマルイラスト16個**を生成
    - 保存先: `assets/images/git-deck/`
    - 対応例: git-001-init.png, git-004-add.png, git-012-tag.png, git-045-stash.png など
- 「２（CSVを16個で一旦確定）＋４（import側対応）」を実行
  - git_deck.csv を現在の生成済み16個で確定状態に更新
  - `import_to_anki.py` を新規作成
    - CSV読み込み（3列対応）
    - 画像ファイルが存在する場合に `storeMediaFile` でAnkiメディアへアップロード
    - GitカードのFrontに質問文＋`<img src="...">` を埋め込んで追加
    - デッキ自動作成、進捗表示、エラーハンドリング
  - `requirements.txt` を作成（requests）
  - plan.md に画像ハンドリングの技術仕様を追記

### 現在の成果物状態（2026-07-08時点）
- `git_deck.csv`：50問 + Image列（16問に実画像パスが設定済み）
- `assets/images/git-deck/`：16個のミニマルイラスト（24色限定フラットスタイル）
- `import_to_anki.py`：画像対応済み（未テスト）
- `generate_git_deck.py`：画像マッピング管理機能付き
- plan.md：ビジュアル要件・インポート仕様が大幅に拡張済み
- 残りイラスト：34個未生成

### 次にすべきこと（優先順）
1. **残り34個のミニマルイラスト生成を継続**（simple-icon-set.jpg スタイルを厳守しつつ、Git概念ごとに視覚的にわかりやすいアイコンを作成）
2. **import_to_anki.py の動作確認**
   - Anki + AnkiConnectを起動して実際に実行
   - 画像が正しくアップロードされ、カードのFrontに表示されるか確認
   - 画像がないカードの扱い（スキップ or プレースホルダー）を改善
3. **全50個画像完成後**
   - CSVを最終確定
   - importスクリプトで全カード＋全画像をAnkiに投入してテスト
4. **Task 2（math_deck.csv）** に着手
5. **Task 4（ガイド出力）** の準備（READMEや実行手順の整備）
6. 必要に応じて画像のクオリティ調整や、カードへの画像配置の最適化（Frontのみ / 別フィールド化 など）

---

**（ここまでがGrokによるplan.md追記仕様 + 2026-07-08進捗記録）**

---

## 要件4：Emacsキー操作デッキ（77問）— 仕様追記（2026-07-09）

### 4.0 概要

| 項目 | 内容 |
|------|------|
| デッキ名（Anki） | `Emacsキー操作` |
| 出力CSV | `emacs_deck.csv` |
| 生成スクリプト | `generate_emacs_deck.py` |
| ノートタイプ | Anki 標準 **Basic**（Gitデッキと同様） |
| カード数 | **77問**（一次資料どおり） |
| インポート | `import_to_anki.py` に `--emacs-only` 等を追加して対応 |

**目的**: Emacs のキーバインド・コマンドを「実行したい操作 → キー／コマンド名」で瞬時に想起できるようにする。数学デッキと同様、**1問5秒以内**を目標とするが、表面の文は自然な日本語（やや大き目）で記述する。

**参照資料（2種類）**:
1. **本デッキの一次資料**: 下記「Emacsキー操作フラッシュカード（全77問）」表
2. **補足資料**: 「Avy ＆ Embark キー操作フラッシュカード表」（Avyディスパッチ・Embarkアクションの詳細版。77問には要約版が含まれる）

---

### 4.1 CSVフォーマット

```csv
"表面","裏面","カテゴリ"
"1文字左に移動する","C-b","基本・移動"
```

| カラム | 説明 |
|--------|------|
| 表面 | 実行したい操作・やりたいこと（問題文） |
| 裏面 | キー（`C-b` 等）またはコマンド名（`avy-goto-char-timer` 等） |
| カテゴリ | 分類タグ（Ankiタグにも流用） |

**Ankiフィールドへのマッピング**:

| CSVカラム | Anki Basic フィールド |
|-----------|----------------------|
| 表面 | `Front` |
| 裏面 | `Back` |
| カテゴリ | タグ（`emacs` + カテゴリ名 + `anki-mydeck-make`） |

- 区切り: **カンマ**（`import_to_anki.py` / Anki 手動インポートと同じ）
- フィールドにカンマを含む場合は **ダブルクォートで囲む**（`generate_emacs_deck.py` は `csv.QUOTE_ALL` で出力）
- 文字コード: UTF-8
- 裏面に複数キー候補がある場合はそのまま記載（例: `C-/ または C-_`）
- Magit の連続キーはスペース区切り（例: `d d`, `c c`）
- **画像列は不要**（Gitデッキとは異なる）

**Grok への生成依頼プロンプト例**:

```text
以下の表はEmacsのキー操作をまとめたものです。この表のデータを使って、
AnkiアプリにインポートできるCSV形式（表面, 裏面, カテゴリ）のテキストデータを作成してください。
3列構成・UTF-8・必要に応じてダブルクォートで囲んだ標準CSVにしてください。
表面は自然な日本語の問題文、裏面はキーまたはコマンド名のみとし、学習しやすい形に整えてください。
```

---

### 4.2 カテゴリ別配分（77問）

| カテゴリ | 問数 | 内容 |
|----------|------|------|
| 基本・移動 | 10 | C-b / C-f / C-p / C-n / C-a / C-e / M-v / C-v / M-< / M-> |
| 基本・編集 | 8 | 全選択・コピー・切取・貼付・ヤンクポップ・行キル・文字削除・Undo |
| 基本・検索 | 2 | C-s / C-r |
| 基本・ウィンドウ | 3 | C-x 2 / C-x 3 / C-x 1 |
| 基本・ファイル | 3 | C-x C-f / C-x C-s / C-x b |
| 基本・その他 | 2 | C-g / C-x C-c |
| Org-mode | 20 | 見出し・TODO・日付・タグ・表・アジェンダ等 |
| Magit | 16 | ステータス・ステージ・コミット・ブランチ・ログ・push/pull 等 |
| Avy | 5 | ジャンプ・ディスパッチ一覧・kill/copy/zap（カテゴリ `Avy`） |
| Avy/Embark | 1 | Avyディスパッチから Embark を呼び出す（カテゴリ `Avy/Embark`） |
| Embark | 4 | embark-act / export・代表アクション |
| Paredit | 3 | C-k / Slurp / Barf |
| **合計** | **77** | （10+8+2+3+3+2+20+16+5+1+4+3） |

※ Avy / Embark の**詳細版**（ディスパッチ全キー等）は補足表に記載。将来 `emacs_avy_embark_deck.csv` として別デッキ化も可。

---

### 4.3 一次資料：Emacsキー操作フラッシュカード（全77問）

#### 基本・移動（10問）

| 表面（問題） | 裏面（解答） | カテゴリ |
|-------------|-------------|----------|
| 1文字左に移動する | C-b | 基本・移動 |
| 1文字右に移動する | C-f | 基本・移動 |
| 1行上に移動する | C-p | 基本・移動 |
| 1行下に移動する | C-n | 基本・移動 |
| 行頭に移動する | C-a | 基本・移動 |
| 行末に移動する | C-e | 基本・移動 |
| 1ページ戻る | M-v | 基本・移動 |
| 1ページ進む | C-v | 基本・移動 |
| ファイル先頭に移動する | M-< | 基本・移動 |
| ファイル末尾に移動する | M-> | 基本・移動 |

#### 基本・編集（8問）

| 表面（問題） | 裏面（解答） | カテゴリ |
|-------------|-------------|----------|
| テキスト全選択 | C-x h | 基本・編集 |
| 選択範囲をコピーする（キルリングに保存） | M-w | 基本・編集 |
| 選択範囲を切り取る（キル） | C-w | 基本・編集 |
| 貼り付け（ヤンク） | C-y | 基本・編集 |
| 履歴から貼り付ける（ヤンクポップ） | M-y | 基本・編集 |
| 1行切り取る | C-k | 基本・編集 |
| 1文字削除する（デリート） | C-d | 基本・編集 |
| 操作を元に戻す（Undo） | C-/ または C-_ | 基本・編集 |

#### 基本・検索（2問）／ウィンドウ（3問）／ファイル（3問）／その他（2問）

| 表面（問題） | 裏面（解答） | カテゴリ |
|-------------|-------------|----------|
| カーソルより下の範囲で検索（インクリメンタル検索） | C-s | 基本・検索 |
| カーソルより上の範囲で検索 | C-r | 基本・検索 |
| ウィンドウを上下に分割する | C-x 2 | 基本・ウィンドウ |
| ウィンドウを左右に分割する | C-x 3 | 基本・ウィンドウ |
| 分割されたウィンドウを現在の1つだけにする | C-x 1 | 基本・ウィンドウ |
| ファイルを開く | C-x C-f | 基本・ファイル |
| ファイルを保存する | C-x C-s | 基本・ファイル |
| バッファを変更・切り替える | C-x b | 基本・ファイル |
| 実行途中のコマンドを中断（アボート）する | C-g | 基本・その他 |
| Emacsを終了する | C-x C-c | 基本・その他 |

#### Org-mode（20問）

| 表面（問題） | 裏面（解答） | カテゴリ |
|-------------|-------------|----------|
| 【Org】見出しを隠す / 展開する | TAB | Org-mode |
| 【Org】すべてを展開して表示する | C-u C-u C-u TAB | Org-mode |
| 【Org】新しい見出しを追加する | M-RET | Org-mode |
| 【Org】見出しのレベルを減らす（昇格） | M-LEFT | Org-mode |
| 【Org】見出しのレベルを増やす（降格） | M-RIGHT | Org-mode |
| 【Org】サブツリー全体を上に移動する | M-UP | Org-mode |
| 【Org】サブツリー全体を下に移動する | M-DOWN | Org-mode |
| 【Org】TODOの状態を切り替える | C-c C-t | Org-mode |
| 【Org】優先度を設定する | C-c , | Org-mode |
| 【Org】チェックボックスの状態をトグルする | C-c C-c | Org-mode |
| 【Org】スケジュール（作業予定日）を設定する | C-c C-s | Org-mode |
| 【Org】締め切り（DEADLINE）を設定する | C-c C-d | Org-mode |
| 【Org】タイムスタンプを挿入する | C-c . | Org-mode |
| 【Org】タグを設定する | C-c C-q | Org-mode |
| 【Org】プロパティを設定する | C-c C-x p | Org-mode |
| 【Org】クロッキング（作業時間記録）を開始する | C-c C-x C-i | Org-mode |
| 【Org】クロッキングを停止する | C-c C-x C-o | Org-mode |
| 【Org】条件に合う見出しのスパースツリーを作成する | C-c / | Org-mode |
| 【Org】表を再フォーマット（アライメント）する | C-c C-c または TAB | Org-mode |
| 【Org】アジェンダディスパッチャ（一覧）を開く | C-c a (一般的な割当) | Org-mode |

#### Magit（16問）

| 表面（問題） | 裏面（解答） | カテゴリ |
|-------------|-------------|----------|
| 【Magit】ステータス画面を開く | magit-status | Magit |
| 【Magit】カーソル位置のファイルをステージングする | s | Magit |
| 【Magit】カーソル位置のファイルのステージングを解除する | u | Magit |
| 【Magit】すべてのファイルをステージングする | S | Magit |
| 【Magit】すべてのファイルのステージングを解除する | U | Magit |
| 【Magit】ファイルの差分をインラインで展開/折りたたみする | TAB | Magit |
| 【Magit】カーソル位置のファイルの変更を取り消す（破棄する） | k | Magit |
| 【Magit】ワーキングツリーの差分を表示する | d d | Magit |
| 【Magit】コミットメニューを開く/コミットを作成する | c c | Magit |
| 【Magit】直前のコミットを修正する（amend） | c a | Magit |
| 【Magit】ブランチを切り替える（checkout） | b b | Magit |
| 【Magit】新しいブランチを作成して切り替える | b c | Magit |
| 【Magit】ブランチを削除する | b d | Magit |
| 【Magit】現在のブランチのログを確認する | l l | Magit |
| 【Magit】リモートリポジトリにプッシュする | P p | Magit |
| 【Magit】リモートリポジトリからプルする | F p | Magit |

#### Avy（6問）／Embark（4問）／Paredit（3問）— 77問版に含まれる要約

| 表面（問題） | 裏面（解答） | カテゴリ |
|-------------|-------------|----------|
| 【Avy】画面上の文字列にタイマーベースでジャンプする | avy-goto-char-timer | Avy |
| 【Avy】実行可能なアクション(ディスパッチ)の一覧を表示する | ? | Avy |
| 【Avyディスパッチ】対象の単語やsexpを削除(Kill)する | k | Avy |
| 【Avyディスパッチ】対象の単語やsexpをコピー(Copy)する | w | Avy |
| 【Avyディスパッチ】現在のカーソル位置から対象までを削除(Zap)する | z | Avy |
| 【Avyディスパッチ】対象に対してEmbarkのアクションを呼び出す | o | Avy/Embark |
| 【Embark】選択した対象に対してアクションを選択・実行する | embark-act | Embark |
| 【Embark】ミニバッファーの候補リストから永続的なコレクションを作成 | embark-export | Embark |
| 【Embarkアクション】ファイルのオープン時に別の場所へコピーする | c | Embark |
| 【Embarkアクション】root権限(sudo)でファイルをオープンする | S | Embark |
| 【Paredit】S式を削除(Kill)し、括弧の構造を保持する | C-k | Paredit |
| 【Paredit】次のS式を現在のリストの中に取り込む(Slurp) | C-) または C-<right> | Paredit |
| 【Paredit】現在のリストの最後のS式を外に出す(Barf) | C-} または C-<left> | Paredit |

---

### 4.4 補足資料：Avy ＆ Embark 詳細表（将来拡張用）

77問デッキでは Avy / Embark は要約のみ。以下は**全ディスパッチ・アクション**を網羅した拡張版（別デッキ候補）。

#### Avy（推奨コマンド＋ディスパッチ）

| 表面 | 裏面 | カテゴリ |
|------|------|----------|
| 画面上の任意の文字列にタイマーベースでジャンプする (Avyの推奨コマンド) | avy-goto-char-timer | Avy |
| Avy実行中、実行可能なアクション(ディスパッチ)の一覧を表示する | ? | Avy |
| 【Avyディスパッチ】対象の単語やsexpを削除(Kill)する | k | Avy |
| 【Avyディスパッチ】対象を行ごと削除する | K | Avy |
| 【Avyディスパッチ】対象の単語やsexpを現在のカーソル位置にコピー(Yank)する | y | Avy |
| 【Avyディスパッチ】対象を行ごと現在のカーソル位置にコピー(Yank)する | Y | Avy |
| 【Avyディスパッチ】対象の単語やsexpをコピー(Copy)する | w | Avy |
| 【Avyディスパッチ】対象を行ごとコピー(Copy)する | W | Avy |
| 【Avyディスパッチ】対象の単語やsexpを現在のカーソル位置に移動(Teleport)させる | t | Avy |
| 【Avyディスパッチ】対象の行を現在のカーソル位置に移動(Teleport)させる | T | Avy |
| 【Avyディスパッチ】現在のカーソル位置から対象位置までを削除(Zap)する | z | Avy |
| 【Avyディスパッチ】対象の単語やsexpをマークする | m | Avy |
| 【Avyディスパッチ】現在のポイントから対象位置までのリージョンを選択する | SPC | Avy |
| 【Avyディスパッチ】対象単語のスペルチェック・修正(Ispell/Flyspell)を行う | i または ; | Avy |
| 【Avyディスパッチ】対象単語の意味を辞書検索する | = | Avy |
| 【Avyディスパッチ】対象シンボルのドキュメント(Help)を調べる | H | Avy |
| 【Avyディスパッチ】対象文字列をGoogle検索する | C-= | Avy |
| 【Avyディスパッチ】対象に対してEmbarkのアクションメニューを呼び出す | o | Avy / Embark |

#### Embark（基本＋アクション）

| 表面 | 裏面 | カテゴリ |
|------|------|----------|
| 選択した対象(ファイルやバッファ等)に対してアクションを選択・実行する (Embarkの基本コマンド) | embark-act | Embark |
| ミニバッファーの候補リストから、永続的なコレクションを作成・エクスポートする | embark-export | Embark |
| ミニバッファーの候補リストから、永続的なコレクションバッファーを直接作成する | embark-collect | Embark |
| Embarkのアクションメニュー表示中、アクションリストと候補リストの表示を切り替える | TAB | Embark |
| プロンプトを残したまま（セッションを継続して）Embarkのアクションを実行する | プレフィックス引数を付けて embark-act を実行（例: C-u + embark-actのバインドキー） | Embark |
| 【Embarkアクション】ファイルのオープン時に別の場所へコピー(copy-file)する | c | Embark |
| 【Embarkアクション】ミニバッファーで選択したテキストをメインのバッファーに挿入する | i | Embark |
| 【Embarkアクション】選択したファイル候補に対して非同期シェルコマンド(async-shell-command)を実行する | & | Embark |
| 【Embarkアクション】root権限(sudo)でファイルをオープンする | S | Embark |
| 【Embarkアクション】選択したテキストリージョンを0x0にアップロードする | U | Embark |
| 【Embarkアクション】パッケージのURLをブラウザで開く | u | Embark |
| 【Embarkアクション】選択した変数の値をセット(set-variable)する | = | Embark |
| 【Embarkアクション】選択したコマンド名にグローバルなキーバインディングを追加(global-set-key)する | g | Embark |

---

### 4.5 技術仕様（import_to_anki.py 拡張）

```python
EMACS_DECK_NAME = "Emacsキー操作"
EMACS_CSV = "emacs_deck.csv"

# addNote 例（Basic）
{
    "deckName": "Emacsキー操作",
    "modelName": "Basic",
    "fields": {
        "Front": "1文字左に移動する",
        "Back": "C-b"
    },
    "tags": ["emacs", "基本・移動", "anki-mydeck-make"]
}
```

| 項目 | 仕様 |
|------|------|
| CLI オプション | `--emacs-only`（`--git-only` / `--math-only` と**排他的**に `argparse` の mutually exclusive group へ追加） |
| タグ | `emacs` + カテゴリ名（例: `Org-mode`, `Magit`）+ `anki-mydeck-make`。`Avy/Embark` は Anki 上で階層タグ（`Avy` の子）になる |
| 重複防止 | 数学デッキと同様、再実行時は duplicate エラーに注意 |
| 解説（Back Extra） | **不要**（Basic 2フィールドのみ） |

#### 4.5.1 ファイル構成（追加分）

```
my-anki-deck-make/
├── emacs_deck.csv              # 77問（表面, 裏面, カテゴリ）
├── generate_emacs_deck.py      # CSV生成スクリプト
└── import_to_anki.py           # emacs デッキ対応を追加
```

---

### 4.6 タスクリスト（追記）

```text
【タスクリスト — Emacsデッキ】
- [ ] Task 5: `emacs_deck.csv` の生成
  - 本仕様 4.3 の77問を一次資料として CSV 化する。
  - `generate_emacs_deck.py` を作成し、カテゴリ別件数・合計77問を検証する。
- [ ] Task 6: `import_to_anki.py` の Emacs 対応
  - `--emacs-only` オプション追加、Basic ノートでインポート。
  - 事前チェック（preflight）に emacs_deck.csv の存在・行数確認を追加。
- [ ] Task 7: Anki 投入・動作確認
  - デッキ「Emacsキー操作」に77問が登録されることを確認。
- [ ] （任意）Task 8: Avy & Embark 詳細デッキ
  - 本仕様 4.4 の拡張表を別 CSV（**31問**: Avy 18 + Embark 13）として追加デッキ化。
```

---

### 4.7 設計上の注意

1. **Magit / Avy / Embark のキーはコンテキスト依存**: 表面に `【Magit】` `【Avy】` 等のプレフィックスを付け、他カテゴリの同名キー（例: `k`, `c`, `TAB`）と混同しない。
2. **Paredit の C-k** は通常の `C-k`（1行切り取り）と異なる。表面文で「S式」「括弧の構造を保持」と明示する。
3. **Org-mode のキー** はユーザー設定（`C-c a` 等）で差異がありうる。裏面に `(一般的な割当)` と注記済みのものはそのまま維持。
4. **77問の厳守**: 一次資料と生成結果の行数が一致することを `generate_emacs_deck.py` の `validate_cards()` で assert する。
5. **同一裏面キーの共存**: `C-k`（基本・編集＝行キル）と `C-k`（Paredit＝S式キル）、`k`/`c`/`TAB`（Magit/Avy/Embark）など、裏面が同じでも**表面のプレフィックスで区別**する現行設計は意図的。変更しない。
6. **上位仕様との整合**: 本要件追加によりプロジェクト総カード数は **227問**（§4.8）。旧 §6 の DoD（150問）は Emacs 追加前の記述のため、Task 5 完了時に §6 も更新する。

---

### 4.8 プロジェクト全体の更新（デッキ数）

| デッキ | 問数 | 形式 | 状態 |
|--------|------|------|------|
| Git学習 | 50 | Basic + 画像 | 完了 |
| 高校数学・基礎解析 | 100 | Cloze + MathJax + 解説 | 完了 |
| **Emacsキー操作** | **77** | **Basic（3列CSV）** | **未着手（本仕様で定義）** |
| **合計（予定）** | **227** | | |

**（ここまでが要件4：Emacsキー操作デッキ仕様追記 — 2026-07-09）**
